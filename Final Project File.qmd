---
title: "Final Project"
format: html
editor: visual
---

## Project

```{r}

library(tidyverse)
library(ggplot2)

rawdata <- read_csv("data/pollution.csv")

```

Research Question:

```{r}

bycountry <- rawdata |>
  group_by(ParentLocation) |>
  summarize(IndicatorCode)

```
